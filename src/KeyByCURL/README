Тест для тестирования отправки ключа внутри cURL-запроса.

РАБОТА
Тест формирует строку для вызова её через shell вида:
curl --data-ascii <ключ в HEX в виде текстовой строки> ip1:port1
curl --data-ascii <ключ в HEX в виде текстовой строки> ip2:port2

КОМПИЛЯЦИЯ
Запуск build.sh

ИСПОЛЬЗОВАНИЕ
Запуск ./KeyByCURL.out 127.0.0.1:78 127.0.0.1:77
