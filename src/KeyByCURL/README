Тест для тестирования отправки ключа внутри cURL-запроса.

РАБОТА
Тест формирует строку для вызова её через shell вида:
curl --data-ascii <ключ в HEX в виде текстовой строки> http://127.0.0.1:77
curl --data-ascii <ключ в HEX в виде текстовой строки> http://127.0.0.1:78

КОМПИЛЯЦИЯ
Запуск build.sh

ИСПОЛЬЗОВАНИЕ
Запуск KeyByCURL.out.
