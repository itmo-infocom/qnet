Тест для тестирования отправки ключа внутри cURL-запроса.

РАБОТА
Тест формирует строку для вызова curl через shell. В теле cURL-запроса отправляется число бит ключа, символ перевода строки '\n' и сам ключ в шестнадцатеричном виде. 

KeyByCURL.out [URL]
curl --data-ascii "<число бит ключа>\n<ключ в HEX в виде текстовой строки>" http://<URL>/qchannel/1/1

ПРИМЕР
./KeyByCURL.out my_host
curl --data-ascii "40
3f1a251bca http://my_host/qchannel/1/1"

КОМПИЛЯЦИЯ
Запуск build.sh